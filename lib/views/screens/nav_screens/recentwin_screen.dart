import 'package:flutter/material.dart';

class RecentwinScreen extends StatelessWidget {
  const RecentwinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Setting Screen"),
      ),
    );
  }
}